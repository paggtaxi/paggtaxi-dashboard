import { TableOrderingDirective } from './table-ordering.directive';

describe('TableOrderingDirective', () => {
  it('should create an instance', () => {
    const directive = new TableOrderingDirective();
    expect(directive).toBeTruthy();
  });
});
