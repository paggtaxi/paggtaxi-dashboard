import { BooleanImgPipe } from './boolean-img.pipe';

describe('BooleanImgPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanImgPipe();
    expect(pipe).toBeTruthy();
  });
});
